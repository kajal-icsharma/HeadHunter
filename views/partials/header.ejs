
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Head Hunters</title>
    <link rel = "icon" href = 
    "/img/icons8-h-96.png"        type = "image/x-icon">
    <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&family=Playball&family=Tangerine&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    
    
    <!-- Navbar !-->
    <nav class="my-navbar animate__animated animate__backInDown animate__fast ">
        <!-- Logo !-->
        <div>
            <i id="logo" class="fa-sharp fa-solid fa-square-h fa-beat-fade fa-2xl me-2" style="color: #ffffff;"></i>
            
            <a id="brand" href="/">HeadHunters</a>        
        </div>
        <ul class="nav-list">
            <!-- Link 1 !-->
            <li class="nav-item">
                <a class="nav-link" href="/">Home</a>
            </li>
            <!-- Link 2 !-->
            <li class="nav-item">
                <a class="nav-link" href="/jobs">Jobs</a>
            </li>
            <!-- Link 3 !-->
            <li class="nav-item">
                <a class="nav-link" href="/notifications">Notifications</a>
            </li>
            <!-- Link 4 !-->
            <li class="nav-item">
                <a class="nav-link" href="/jobs/new">Add Job</a>
            </li>
            <% if (!currentUser) { %>
            <li class="nav-item">
                <a class="nav-link" href="/login">Login or Register</a>
            </li>
            <% } else { %>
            <li class="nav-item">
                <a class="nav-link" href="/users/<%= currentUser._id %>">Signed in as
                    <%= currentUser.name %></a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/logout">Logout</a>
            </li>
            <% } %>



            
        </ul>
    </nav>


    <% if (success && success.length>0) { %>
        <div class="alert alert-success" role="alert"><%= success %></div>
        <% } %>
        <% if (error && error.length>0) { %>
        <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>




    